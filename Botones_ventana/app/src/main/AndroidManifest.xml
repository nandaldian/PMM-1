<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chrcuc.botones_ventana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Linear" />
        <activity android:name=".Table" />
        <activity android:name=".Relative" />
        <activity android:name=".Grid" />
        <activity android:name=".CheckBox" />
        <activity android:name=".CBEjercicio" />
        <activity android:name=".CBEjercicio2" />
        <activity android:name=".CBEjercicio3"></activity>
    </application>

</manifest>